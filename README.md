# kate
mekeCacheMatrix <- fnction(x = matri()) {
        in <- NULL
        st <- funtion(y) {
                x <<- y
                inv <<- NULL
        }
        get <- functio() x
        setInvrse <- funcion(invers) inv <<- inverse
        getIverse <- funcion() nv
        lis(set = set,
             get = gt,
             setInvese = setInvese,
             getnverse = getInerse()
}
